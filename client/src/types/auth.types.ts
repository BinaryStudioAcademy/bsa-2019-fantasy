export type LoginCredentials = {
  username: string;
  password: string;
};

export type RegisterCredentials = {
  username: string;
  password: string;
  email: string;
};

export type LoginFormProps = {
  location: string;
};
